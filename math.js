const number1 = 2.333;
const number2 = 2.933;
const number3 = 2.500;

var num1 = 10
var num2 = 23
// if difference 5 years bluld frindship
var difference = Math.abs(num1 - num2); // Math.abs() choto bodo number theke boro nuber theke choto numbers bad deya hoi..
if(difference <= 5){
    console.log('built frentship');
} else{
    console.log('No, Dure dure thako');
}
console.log(Math.pow(3,2)); // Math.pow(x,y): kono number er power dear jonno Math function   ti use koro. x holo base y holo power

    var random = Math.random()*6;
    const mathCeil = Math.ceil(random);   // random simply vongaonso number dei. *Math.ceil()* oi dosomic number k tar boro indijer number a ropantor koren random 👉 0.33343434 || Math.ceil 👉 1  
    console.log(Math.ceil(random));
    const goti1 = 34;
    const goti2 = 40;
    const goti3 =  3;
    const goti4 =  4;
    const diffarent = Math.abs(goti3 - goti4);
    console.log(diffarent);
    if (goti3 - goti4 == -1 || goti3 - goti4 == 1) {
        console.log('yes,');
        
    }else{
        console.log('No,');
    }
